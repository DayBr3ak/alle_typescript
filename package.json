{
  "devDependencies": {
    "@types/node": "^10.12.0",
    "nodemon": "^1.18.4",
    "rimraf": "^2.6.2",
    "typescript": "^3.1.3"
  },
  "private": true,
  "scripts": {
    "clean": "rimraf build",
    "build": "tsc -b packages/node_modules",
    "dev": "yarn run clean && nodemon build/node_modules/integration"
  }
}
